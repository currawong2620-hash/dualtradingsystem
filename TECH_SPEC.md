## 📄 TECH_SPEC.md — *Dual-Mode Forex Trading System (v1.2)*

### 1. Overview

**Цель:** создать адаптивного EA для MetaTrader 5 (MQL5), который автоматически определяет рыночный режим и переключает торговую логику между:

* **Trend Engine** — следование за направленным движением;
* **Range Engine** — торговля возвратов к среднему во флэте.

**Режим определяется на старшем TF (H1), торговля ведётся на рабочем TF (M5).**

#### 1.1 Assumptions & Constraints

| Параметр            | Значение                                  |
| ------------------- | ----------------------------------------- |
| Пары                | EURUSD, GBPUSD, USDJPY, XAUUSD            |
| Минимальный депозит | ≥ 1000 USD (0.01 лот, ECN)                |
| Максимум символов   | 3                                         |
| Хеджинг             | Разрешён                                  |
| Спред               | ≤ 2 пункта                                |
| Отказ от гарантий   | Результаты зависят от рынка и ликвидности |

---

### 2. Market Regime Detection (Mode Analyzer)

Метрики и веса: ADX 0.4  | ATR 0.3  | STD 0.2  | Slope 0.1.
Пороги: ADX L=17, H=25; SlopeThr = 0.1; N_confirm = 3.
FSM переходы через `TRANSITION` после 3 подтверждённых баров.

[
bias = Σ w_i · norm(metric_i),\quad
μ = EMA(bias),\ σ = EMA(|bias − μ|),\
confidence = |bias| / (σ+ε)
]

---

### 3. Trend Engine

**TF:** M5
**Входы:** Donchian(20) breakout + ADX_H1>25 + EMA(50/200) alignment.
**Фильтры:** RSI <80/>20, ATR > среднего за 50 баров.
**Выходы:** SL = 1.8·ATR, TP ≈ 3·ATR, ATR-трейлинг.
**Новости:** пауза ±30 мин от High-impact (из CSV или Calendar API).
**Risk per trade:** ≤ 1 % от TrendCapital.

#### 3.1 Pyramiding (optional)

| Параметр           | Default        | Описание                                |
| ------------------ | -------------- | --------------------------------------- |
| Enable_pyramiding  | false          | Опционально (по умолчанию выключено)    |
| Max_add_levels     | 2              | Макс. число добавлений                  |
| Min_profit_for_add | 1.5 ATR        | Добавление только при профите > 1.5 ATR |
| Add_lot_multiplier | 0.5            | Каждое добавление = 0.5 базового лота   |
| Conf_threshold     | 0.7            | Добавление только при conf > 0.7        |
| ATR_condition      | ATR > среднего | Подтверждение силы движения             |

**Логика:**
Если Enable_pyramiding и условия выполнены — `OpenAddPosition()` с новым SL, сдвинутым на basis входа.
**Safety:** общий risk ≤ 2 % (см. раздел 6). При DD > 10 % в сессии — авто-пауза пирамидинга.

---

### 4. Range Engine

**TF:** M5
**Входы:** цена у границы BB(20,2), RSI<30/>70, ADX_H1<17, BBw < среднего.
**Выход:** TP = 0.6 · BBw, SL = 1.2 · ATR.
**Фильтр:** ADX>25 → новые входы запрещены.
**Risk per trade:** ≤ 0.5 % от RangeCapital.

---

### 5. Mode Switch Mechanism (FSM)

| State      | Engine       | Volume           |
| ---------- | ------------ | ---------------- |
| TREND      | Trend Engine | Vtrend × f(conf) |
| RANGE      | Range Engine | Vrange × f(conf) |
| TRANSITION | Pause 3 бара | 0                |

[
f(conf)=min(1,\ 1.2×conf)
]
Debounce = 3 подтверждённых бара.

---

### 6. Risk Management

* **Пулы:** Trend 60 %, Range 40 %.
* **Daily risk limit:** ≤ 2.5 % депозита.
* **Max DD:** 20 % (авто-остановка).
* **Pyramiding:** при Enable — cumulative risk ≤ 2 %, auto-off если DD > 10 % в сессии.
* **Корреляции:** ρ > 0.7 → запрет второго ордера (при pyramiding — порог ρ > 0.6).
* **Slippage/Commission:** в симуляторе μ = 1 пип, σ = 0.5.
* **Error handling:** retry при CopyBuffer и trade timeouts.

---

### 7. Architecture & Modules

(аналогично v1.1, добавлен pyramiding hook в TradeManager.mqh)

---

### 8. Backtesting & Evaluation

* **Период:** 2015–2025 (тик-данные).
* **Метрики:**

  * PF ≥ 1.35
  * Sharpe ≥ 0.8
  * Max DD ≤ 20 % (без pyramiding) / ≤ 25 % (с ним)
  * PF_trend ≥ 1.5, PF_range ≥ 1.25
* **Отдельные прогоны:** с и без пирамидинга для сравнения доходности vs риска.
* **Monte-Carlo:** 1000 итераций для оценки DD и вариативности результатов.

---

### 9. Implementation

Strict MQL5, ECN hedging-allowed, Windows 10/11.
В TradeManager реализовать `CalcCumulativeExposure()` и ограничения для pyramiding.

---

### 10. Future Extensions

1. Новостной модуль (в core).
2. ML ModeAnalyzer (v2.0).
3. Мультисимвольный портфель и динамический риск.
4. Self-optimization через μ ± σ профили.

---

### 11. Revised Targets

| Метрика       | Цель                         |
| ------------- | ---------------------------- |
| Sharpe        | ≥ 0.8                        |
| Profit Factor | ≥ 1.35                       |
| Max DD        | ≤ 20 % (25 % при pyramiding) |
| Hit Rate      | 45–60 %                      |
| Avg R:R       | ≥ 1.8 : 1                    |

---

© 2025 Dual-Mode Forex Project — **v1.2 (Conservative Baseline + Optional Pyramiding)**
